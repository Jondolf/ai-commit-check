name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  no-ai-commits:
    name: No AI Commits
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # This repository dogfoods its own action.
      - name: Check commits for AI authorship
        uses: ./
